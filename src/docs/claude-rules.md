# Project Rules

- Framework: Vue 3
- Using Composition API
- Using script setup
- TailwindCSS
- No inline styles
- Prefer existing UI kit components

# UI Kit Components

uikit components in lib/ui-kit/packages directory

# Design Rules

theme colors and vaiables defined in assets/base.css

# Coding Rules

- Reusable components only
- Avoid duplicated styles
- Mobile first
- Use computed where possible
- Avoid watchers unless necessary